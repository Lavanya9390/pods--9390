apiVersion: v1
kind: Pod
metadata:
  name: svc-pod
  labels:
    app: test
spec:
  containers:
    - name: test-co
      image: nginx:latest
      ports:
        - containerPort: 80
          protocol: TCP